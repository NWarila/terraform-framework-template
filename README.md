# terraform-framework-example
Do-nothing reference framework. Showcases every pattern (modules, variables, outputs, tests, terraform-docs, OPA, drift) with synthetic resources only (null/random/local/time). Generates real tfstate, no external deps. Consumed by terraform-runner-template's example pr-validation.
